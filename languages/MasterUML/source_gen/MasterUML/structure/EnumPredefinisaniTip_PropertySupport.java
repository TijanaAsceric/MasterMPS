package MasterUML.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class EnumPredefinisaniTip_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<EnumPredefinisaniTip> constants = ListSequence.fromList(EnumPredefinisaniTip.getConstants()).iterator();
    while (constants.hasNext()) {
      EnumPredefinisaniTip constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<EnumPredefinisaniTip> constants = ListSequence.fromList(EnumPredefinisaniTip.getConstants()).iterator();
    while (constants.hasNext()) {
      EnumPredefinisaniTip constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    EnumPredefinisaniTip constant = EnumPredefinisaniTip.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
